name: Compile All Three

on:
  workflow_dispatch:       # Manual “Run workflow” button

jobs:
  build-macos:
    uses: milesfleisher/ARIana/.github/workflows/build-mac.yml
    permissions: inherit
    secrets: inherit

  build-ubuntu:
    uses: milesfleisher/ARIana/.github/workflows/build-ubuntu.yml
    permissions: inherit
    secrets: inherit

  build-windows:
    uses: milesfleisher/ARIana/.github/workflows/build-windows.yml
    permissions: inherit
    secrets: inherit
  
